/** Shared horizontal padding for marketing sections. */
export const landingSectionXClassName = "px-4 sm:px-6 lg:px-8";

/** Inner vertical padding for full-width colored sections. */
export const landingColoredSectionYClassName = "py-16 sm:py-20 lg:py-24";

/** Vertical rhythm for the landing page stack (gap only — not per-section py). */
export const landingPageStackClassName =
  "flex flex-col gap-16 sm:gap-20 lg:gap-24 pt-14 sm:pt-16 lg:pt-20 pb-14 sm:pb-16 lg:pb-20";
